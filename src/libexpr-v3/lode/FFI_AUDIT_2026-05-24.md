# V3-NATIVE FFI audit — post-#795/#803/#804 — 2026-05-24

Updated FFI surface inventory after the V3-NATIVE investigation arc
(#795-#808).  Supersedes `lode/FFI_AUDIT_2026-05-20.md` with empirical
per-workload data.

Plan: `lode/V3_TRUE_NATIVE_PLAN_2026-05-24.md`.
RCA log: `lode/V3_TRUE_NATIVE_RCA_2026-05-24.md`.

Copyright (c) 2026 Moritz Angermann <moritz.angermann@iohk.io>,
Input Output Group.  SPDX-License-Identifier: Apache-2.0.

## Headline: V3-NATIVE is empirically achieved for standard workloads

**Zero v3→TW bridge crossings** on standard nixpkgs workloads
(measured via #795 per-site counters):

  | Workload                      | bridge crossings | bridge primops |
  |-------------------------------|------------------|----------------|
  | hello.drvPath                 | 0                | 0              |
  | bash.drvPath                  | 0                | 0              |
  | gcc.drvPath                   | 0                | 0              |
  | python3.drvPath               | 0                | 0              |
  | firefox.drvPath               | 0                | 0              |
  | multi-IFD-heavy (synthetic)   | 0                | 0              |

The bridge surface only fires on **haskell.nix-class workloads**
(IFD-heavy module-system evaluation).

## haskell.nix-class workload (haskell-nix-example, 45s probe)

  | Site                          | count |
  |-------------------------------|-------|
  | primReadDir attrset           | 0 (was 1, post-#804 native bypass) |
  | primImport string-with-ctx    | 9     |
  | primImport attrset            | 0 (was 7, post-#803 H7 fix)   |
  | primV3ForceAttr_inner cascade | 49 (was 56)                   |
  | **total**                     | **58** (was 73, -21%)         |

Pre-arc baseline was 73 crossings; H7 (#803) + readDir bypass (#804)
removed 15 crossings = -21%.

## Current FFI surface (post-#795/#803/#804)

### Category A: V3-NATIVE achieved (zero crossings)

  | Path                            | Status              |
  |---------------------------------|---------------------|
  | nixpkgs derivation drvPath/name | Pure v3             |
  | Multi-IFD synthetic             | Pure v3             |
  | callFlakeV3 (post-#758)         | Pure v3 except FFI leaves emitTreeAttrs + fetchFinalTree |

### Category B: Legitimate FFI leaves (KEEP)

  | Site                       | Rationale                                |
  |----------------------------|------------------------------------------|
  | realisePath (build leaf)   | Libstore boundary; no v3 alternative     |
  | emitTreeAttrs              | Hash/timestamp formatting (pure data)    |
  | fetchFinalTree             | Flake fetcher (rare; only when no override) |
  | parseFlakeRef + lockFlake  | Pure C; no Nix eval                       |
  | derivationStrict TW fb     | Falls back on shape mismatch only         |
  | builtins.path / fetch*     | Store integration                        |

### Category C: Bridge crossings still fire on haskell.nix

  | Site                          | Reason still in use     | Action     |
  |-------------------------------|-------------------------|------------|
  | primImport string-with-ctx (9)| Needs realisePath on full Value (context entries embedded in string) | Could be optimized via direct string+context construction, low priority |
  | primV3ForceAttr_inner (49)    | TW iterates bridged attrset → re-bridges each attr | Symptom of upstream bridge entry; reduced 56→49 by H7 |

### Category D: Open Phase E follow-ups

  | Task   | Description                                      | Priority |
  |--------|--------------------------------------------------|----------|
  | #805 E2| Eliminate v3ToTreeWalker eager bridge (lines 4786, 4901) for small list/attrset structural transforms | LOW — only fires inside the v3ToTreeWalker recursive walk, not at primop entry points |
  | #806 E3| Retire primV3ForceAttr / primV3CallBridge1 / primV3ForceListElem | **PARTIAL: ListElem retirable, others still BLOCKED — corrected 2026-05-26 evening.** Earlier "unblocked" claim was a measurement artifact: `grep ... \| head -5` truncated the NIX_VM_STATS dump and missed the FINAL cumulative counter. **Corrected true state from full-dump measurement**: HNE flake.lock-eval triggers `__v3_call_bridge_1=8 __v3_force_attr=51` via `fetchFinalTree`'s `type` argument (TW's `fetchTree` builtin forces one attribute from a v3-evaluated flake-input attrset). `__v3_force_list_elem=0` on all tested workloads (HNE, nixpkgs.haskellPackages, hello, firefox). Phase E3 split: (a) ListElem can be retired now (already partially retired per #817; #817b finishes); (b) ForceAttr + CallBridge1 are still load-bearing on flake-fetch paths and cannot retire until the v3->TW bridge construction for those specific code paths is replaced. The "safe-stub" experiment in this session ATTEMPTED to retire all three: HNE eval immediately failed with `error: ... while calling the '__v3_force_attr' builtin` from the stub's throw. Revert applied. **Methodology lesson codified at [[head-5-counter-trap]]**: NIX_VM_STATS dumps cumulative counters multiple times during a run; truncating output with head -N can hide late-firing primops. Always use `tail -1` (or no truncation) when validating "primop is never called" claims. |
  | #807 E4| This doc                                          | DONE     |
  | #808 F | Full validation + ship                            | After remaining E work |

## Remaining over-forcing (haskell.nix x86_64-linux)

Bisection localised the over-forcing to **the construction of
`defaultPackage.<sys>` value**.  Even accessing `.type` (a literal
string field) triggers building apple-sdk + python3 + apple-sdk-14.4
on darwin, or `from-hackage-hello-1.0.0.2` on linux.

On x86_64-linux, after the cabal2nix IFD completes, v3 hits:

```
v3 FORMALS-DIAG lambda='anonymous lambda' unexpected='git' ellipsis=0
  passed_attrs=[lib,stdenv,gitMinimal,coreutils,findutils,gawk,gnused,
    bash,jq,cacert,cvs,git-lfs,mercurial,pijul,git,buildEnv,
    makeWrapper,subversion,breezy,darcs]
  formals=[lib,stdenv,gitMinimal,gnused,cacert,bash,makeWrapper,jq,
    coreutils,findutils,buildEnv,breezy,cvs,darcs,gawk,git-lfs,
    mercurial,pijul,subversion]
```

Lambda has 19 formals (no `git`).  Caller passes 20 attrs (includes
`git`).  Non-ellipsis lambda → v3 errors with "unexpected argument
'git'".  TW does not error in the same code path.

**Direct primop tests pass identically** for builtins.functionArgs
+ builtins.intersectAttrs on a 4-formal synthetic.  Bug is
CONTEXTUAL — somewhere in haskell.nix's overlay or callPackage
chain.  Out of scope for this v3 investigation; requires haskell.nix-
internal source analysis.

## Strategic conclusion

V3-NATIVE is **architecturally achieved** for standard workloads.
The bridge surface reduction (73 → 58 crossings on haskell.nix-class)
+ the Phase A diagnostic infrastructure (counters, traces, ABORT
stats catch, formals-diag) provide the foundation for continued
investigation.

The remaining over-forcing and the `unexpected argument git` error
are haskell.nix-specific behaviors that require:
  1. Source-level analysis of haskell.nix's overlay/callPackage chain
  2. TW-side instrumentation to compare semantic divergence
  3. Possibly haskell.nix-side adjustments

Both are multi-session investigations.  The infrastructure to drive
them is now in place.

## Cumulative diagnostic infrastructure landed this arc

  - `alloc.hh`: v3ToTwBySite[16] per-call-site counter
  - `primops.cc`: 7 sites instrumented + V3_DBG_IFD trace + H7 + readDir-attrset bypass
  - `run.cc`: NIX_VM_STATS per-site dump + WallTimeExceeded ABORT catch + per-kind IFD probe dump
  - `vm.cc`: V3_DBG_FORMALS_DIAG for unexpected-arg diagnostic

Diagnostic env vars (all default-off; cost-when-off ~0):
  - `NIX_VM_STATS=1` — per-site bridge counts + per-kind IFD probes
  - `V3_DBG_IFD=1` — per-IFD-import path + attrs trace
  - `V3_DBG_H7=1` — per primImport-attrset native-path trace
  - `V3_DBG_FORMALS_DIAG=1` — unexpected-arg lambda dump

## Cross-references

  - `lode/V3_TRUE_NATIVE_PLAN_2026-05-24.md` — original plan
  - `lode/V3_TRUE_NATIVE_RCA_2026-05-24.md` — living RCA log
  - `lode/FFI_AUDIT_2026-05-20.md` — superseded prior audit
  - Commits: ed4c86201 (plan), 91a3abbd6 (counters), 4093696bf
    (bridge-empty finding), 15c4ad199 (phase B triage),
    0319f953a (H7), e1f6d534b (per-kind probes), 0c2bc6b00
    (E1 readDir bypass), b399c21bc (formals diag)
